<?xml version="1.0" encoding="utf-8"?>
<!--
  Autorise le HTTP en clair uniquement vers l'hôte de dev (l'API tourne en
  local sans certificat pendant le développement). 10.0.2.2 est l'alias de
  l'émulateur Android vers la machine hôte. À restreindre/retirer une fois
  l'API exposée en HTTPS pour un build de production.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
